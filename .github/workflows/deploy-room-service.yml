name: Deploy Room Service

on:
  push:
    branches: [ main, master ]
    paths:
      - 'MeetingRoomBackend/room-service/**'
      - '.github/workflows/deploy-room-service.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build Room Service JAR
        run: |
          cd MeetingRoomBackend
          mvn clean package -pl room-service -am -DskipTests

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build & Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./MeetingRoomBackend/room-service
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/meetingroom-room-service:latest
            ${{ secrets.DOCKER_HUB_USERNAME }}/meetingroom-room-service:${{ github.sha }}

      - name: Deploy Container to AWS EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/meetingroom-room-service:latest
            sudo docker stop room-service || true
            sudo docker rm room-service || true
            sudo docker run -d \
              --name room-service \
              --restart always \
              --network meetingroom-network \
              -p 8082:8082 \
              -e SPRING_PROFILES_ACTIVE=prod \
              -e EUREKA_SERVER_URL=http://${{ secrets.EUREKA_USERNAME }}:${{ secrets.EUREKA_PASSWORD }}@eureka-server:8761/eureka/ \
              -e CONFIG_SERVER_URL=http://config-server:8888 \
              -e RDS_ENDPOINT="${{ secrets.RDS_ENDPOINT }}" \
              -e RDS_PORT="${{ secrets.RDS_PORT }}" \
              -e RDS_DB_NAME="${{ secrets.RDS_DB_NAME }}" \
              -e RDS_USERNAME="${{ secrets.RDS_USERNAME }}" \
              -e RDS_PASSWORD="${{ secrets.RDS_PASSWORD }}" \
              ${{ secrets.DOCKER_HUB_USERNAME }}/meetingroom-room-service:latest
