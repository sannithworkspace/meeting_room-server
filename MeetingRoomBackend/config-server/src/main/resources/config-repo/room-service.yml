spring:
  datasource:
    url: jdbc:mysql://${RDS_ENDPOINT:database-1.c96i6y2g0vfl.eu-north-1.rds.amazonaws.com}:${RDS_PORT:3306}/${RDS_DB_NAME:meetingroomdb}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${RDS_USERNAME:admin}
    password: ${RDS_PASSWORD:Meetingroom123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

aws:
  s3:
    access-key: ${AWS_ACCESS_KEY:}
    secret-key: ${AWS_SECRET_KEY:}
    bucket-name: ${AWS_S3_BUCKET:meeting-room-s3bucket}
    region: ${AWS_S3_REGION:eu-north-1}
