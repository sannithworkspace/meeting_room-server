spring:
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
  datasource:
    url: jdbc:mysql://${RDS_ENDPOINT:database-1.c96i6y2g0vfl.eu-north-1.rds.amazonaws.com}:${RDS_PORT:3306}/${RDS_DB_NAME:mysql}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${RDS_USERNAME:admin}
    password: ${RDS_PASSWORD:Meetingroom123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://admin:admin123@eureka-server:8761/eureka/}

logging:
  level:
    com.meetingroom.room: INFO
    org.springframework.web: WARN
